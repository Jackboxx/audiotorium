syntax = "proto3";
package home_audio_manager;

message Empty {}

service RpcSourceService {
    rpc GetSources (Empty) returns (GetSourcesResponse);
}

message GetSourcesResponse {
    repeated string names = 1;
}
